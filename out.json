{
  "task": "tensor_division",
  "imports": "import torch",
  "kernel_name": "div_kernel",
  "kernel_code": "import torch\n@triton.jit\ndef div_kernel(input_ptr, other_ptr, output_ptr, n_elements, rounding_mode):\n    pid = tl.program_id(0)\n    block_start = pid * BLOCK_SIZE\n    offsets = block_start + tl.arange(0, BLOCK_SIZE)\n    mask = offsets < n_elements\n    input = tl.load(input_ptr + offsets, mask=mask)\n    other = tl.load(other_ptr + offsets, mask=mask)\n    result = input / other\n    if rounding_mode == 'trunc':\n        result = tl.where(result >= 0, tl.floor(result), tl.ceil(result))\n    elif rounding_mode == 'floor':\n        result = tl.floor(result)\n    tl.store(output_ptr + offsets, result, mask=mask)",
  "launcher_name": "div_launcher",
  "launcher_code": "def div(input, other, *, rounding_mode=None, out=None):\n    if isinstance(other, (int, float, complex)):\n        other = torch.tensor(other, dtype=input.dtype, device=input.device)\n    if input.shape != other.shape:\n        input, other = torch.broadcast_tensors(input, other)\n    \n    if out is None:\n        out = torch.empty_like(input)\n    \n    n_elements = input.numel()\n    BLOCK_SIZE = 1024\n    grid = (n_elements + BLOCK_SIZE - 1) // BLOCK_SIZE\n    \n    div_kernel[grid](input.data_ptr(), other.data_ptr(), out.data_ptr(), n_elements, rounding_mode)\n    return out",
  "constraints": {
    "dtype": "supports_integer_float_complex",
    "device": "cuda",
    "block_size": 1024,
    "extra": {
      "broadcasting": true,
      "scalar_support": true,
      "rounding_modes": [
        "trunc",
        "floor"
      ]
    }
  },
  "quick_test_code": "import torch\na = torch.tensor([10, 20, 30], dtype=torch.float32)\nb = torch.tensor([2, 4, 5], dtype=torch.float32)\nresult = div(a, b, rounding_mode='trunc')\nprint(result)"
}
